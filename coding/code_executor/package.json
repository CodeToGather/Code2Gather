{
    "name": "code-executor",
    "version": "0.0.1",
    "description": "",
    "private": true,
    "devDependencies": {
        "lint-staged": "^11.1.2"
    },
    "scripts": {
        "preinstall": "is-ci || poetry install",
        "export-requirements": "poetry export --without-hashes -f requirements.txt > requirements.txt",
        "lint": "poetry run black . && poetry run isort . && poetry run flake8 --select=F && poetry run mypy ."
    },
    "lint-staged": {
        "**/*.py": [
            "poetry run black",
            "poetry run isort",
            "poetry run flake8 --select=F",
            "poetry run mypy"
        ]
    }
}