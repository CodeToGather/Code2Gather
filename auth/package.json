{
  "name": "auth",
  "version": "0.0.1",
  "description": "Authentication service for Code2Gather",
  "private": true,
  "scripts": {
    "start": "nodemon index.js",
    "postinstall": "",
    "lint": "eslint . --fix --ext .js && prettier --write \"**/*.js\" --tab-width 2",
    "lint:ci": "eslint . --ext .js && prettier --check \"**/*.js\" --tab-width 2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "firebase-admin": "^9.12.0",
    "http-status-codes": "^2.1.4",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.13"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "is-ci": "^3.0.0",
    "lint-staged": "^11.1.2",
    "prettier": "^2.4.1",
    "pretty-quick": "^3.1.1"
  },
  "lint-staged": {
    "**/*.js": [
      "pretty-quick --staged",
      "eslint --fix"
    ]
  }
}
