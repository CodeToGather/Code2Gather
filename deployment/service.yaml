apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  labels:
    run: gateway
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: gateway
  selector:
    run: gateway
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    run: auth
spec:
  type: NodePort
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: auth
  selector:
    run: auth
---
apiVersion: v1
kind: Service
metadata:
  name: history-service
  labels:
    run: history
spec:
  type: NodePort
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
      name: history
  selector:
    run: history
---
apiVersion: v1
kind: Service
metadata:
  name: pairing-service
  labels:
    run: pairing
spec:
  type: NodePort
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: pairing
  selector:
    run: pairing
---
apiVersion: v1
kind: Service
metadata:
  name: video-service
  labels:
    run: video
spec:
  type: NodePort
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
      name: video
  selector:
    run: video